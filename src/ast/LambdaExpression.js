export default class LambdaExpression {
    constructor(components) {
        Object.assign(this, components);
    }
}

export class LambdaParamList {
    constructor(components) {
        Object.assign(this, components);
    }
}
